from __future__ import annotations

from pathlib import Path

RUNTIME_ROOT = Path("investing-os/system/runtime")
SESSIONS_DIR = RUNTIME_ROOT / "sessions"
INPUTS_DIR = RUNTIME_ROOT / "inputs"
PACKETS_DIR = RUNTIME_ROOT / "packets"
