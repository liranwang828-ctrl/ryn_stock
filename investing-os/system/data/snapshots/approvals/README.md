# Snapshot Approvals

Approval records document why a runtime snapshot was allowed to become active.

Every active snapshot must have a corresponding approval record.

Use `snapshot-approval-record-template.md` when approving a snapshot.

