# DAILY 契约 v2 高阶审核记录

日期：2026-06-30
审核对象：commit `d6c5aa0`
结论：`changes_required_minor`

## R1–R6 复核

| 原阻断 | 复核结果 |
|---|---|
| R1 不存在的 plan approval 跃迁 bug | 已从 v2 契约和当前状态台账移除；历史审计与审核记录继续保留原始错误及纠正过程 |
| R2 缓存冒充 IBKR 正式事实 | 已修正为 `stale_unverified` 只读参考，不得完成正式确认、授予权限或完成正式复盘 |
| R3 observation 被截断 | 已恢复 DAILY-2/3 只读事实路径，并明确无交易权限 |
| R4 低阶模型提前作决定 | 已降级为候选修正并分类 |
| R5 经验候选越过 Brain–Muscle 边界 | 已拆分为 stock_team 事实、investing-os 解释与候选、用户确认 |
| R6 全局 provenance schema 未经批准 | 已降级为设计候选，要求先盘点和评估迁移 |

v2 对 R1–R6 的修订方向通过。

## 仍需修正

### V2-1 — C1、C3 不是 `verified_bug`

`init-day` 当前只创建 trading session，是协调器基础阶段的已知范围，不是一个行为与既有测试相反的 bug。`REVIEW_REQUIRED` 已在模型中定义但尚未接入转换，同样属于缺失能力。

调整：

- C1：`verified_bug` → `missing_capability`
- C3：`verified_bug` → `missing_capability`

C8 的静默全量 watchlist 回退违反已经确认的焦点池边界，可以保留为 `verified_bug`。

### V2-2 — 时间窗口尚未决定，不能作为硬进入条件

DAILY-1、DAILY-2、DAILY-3 分别把盘前或交易时段写成进入条件，但 Q7 又询问硬拒绝还是软提示。

在 Q7 获批前，应写为“目标时间窗口”。窗口外行为由 Q7 决定，不得提前定义为不可进入。

### V2-3 — “从所有文件删除 H4”表述不准确

历史审计报告和首次审核记录必须保留 H4 的错误结论及纠正证据，不能改写历史。

状态台账应改成：

> H4 已从当前契约和当前待实施清单移除；历史审计与审核记录保留纠正谱系。

### V2-4 — 七个问题没有区分用户决策与高阶技术决策

行动纲领规定用户决定意义、权限、例外和体验；高阶模型负责架构、状态机和跨系统契约。不能把所有技术选择都交给用户。

## 高阶模型推荐结论

以下项目由高阶模型提出推荐方案，写入修订版后由用户整体批准，不再要求用户逐项设计技术细节。

### Q1 跨日恢复

三种选项都是产品必须支持的用户路径，不是三选一。

推荐实施顺序：

1. 冻结收尾：最小解除昨日阻塞并创建欠账；
2. 快速复盘：完成最低风险检查；
3. 完整复盘：复用 DAILY-4。

界面最终必须同时提供三种选择。

### Q2 observation 状态

推荐创建独立 `OBSERVATION_ACTIVE` 状态。

理由：复用 `INTRADAY_ACTIVE` 容易把“正在交易”和“只读观察”混淆；独立状态更容易实施权限拒绝、Dashboard 标识和恢复测试。

### Q4 非交易模式入口

推荐协调器支持统一 session/action contract；CLI 和 Dashboard 都只是该契约的入口。

第一阶段先提供 CLI 以便测试和恢复，Dashboard 随后接入同一动作。不是二选一。

### Q5 confirm 产物

推荐采用：

```text
investing-os skill / 对话生成认知与计划产物
→ 用户确认
→ coordinator 只做 schema、引用、版本和内容一致性校验
```

CLI 不得自动编造焦点判断、风险计划或用户结论。

## 真正需要用户确认的三项

### U1 `post_open_adj`

高阶推荐：移除其覆盖盘前锚点的能力。盘中若需调整锚点，回到计划变更与用户确认流程，保留版本和审计记录。

### U2 复盘交互

高阶推荐：对话为主、结构化产物为结果。用户通过对话完成判断，investing-os 把结果写入固定 schema；Dashboard 后续只展示和辅助确认，不要求用户在 CLI 表单中重复填写。

### U3 时间窗口

高阶推荐：软提示而非硬拒绝。系统记录目标窗口和迟到状态，但允许跨时区、迟到恢复、观察和补做；数据真实性仍由 as-of、市场窗口和 freshness 单独严格校验。

## 接受条件

修订版 v3 需要：

1. 完成 V2-1 至 V2-3；
2. 把 Q1/Q2/Q4/Q5 替换为上述高阶推荐；
3. 只把 U1–U3 留给用户确认；
4. 不修改业务代码；
5. 保持状态为“待用户批准”。

完成后，高阶模型可把 U1–U3 一次提交用户确认；用户确认后才将契约标记为 approved 并编写实现计划。
